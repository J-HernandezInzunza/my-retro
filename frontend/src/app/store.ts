// Placeholder for future state management
// This will be expanded when we implement Pinia for state management

export interface AppState {
  // Global application state will be defined here
  initialized: boolean
}

// For now, we'll export a simple store placeholder
export const store = {
  // Future store implementation
  state: {
    initialized: false,
  } as AppState,
}

export default store
